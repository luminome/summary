{
  "name": "summary",
  "version": "1.0.0",
  "description": "Utility that uses madge to build a dependency tree.",
  "main": "./build/",
  "scripts": {
    "default-test": "echo \"Error: no test specified\" && exit 1",
    "test": "ts-node src/test.ts",
    "preinstall": "echo \"preparing summary module\" ",
    "install": "echo \"summary module installed (use out)\" && tsc -d -p ."
  },
  "author": "SAC",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.1.6",
    "@types/fs-extra": "^11.0.1",
    "@types/madge": "^5.0.0",
    "@types/node": "^18.11.18",
    "fs-extra": "^11.1.1",
    "madge": "^6.1.0",
    "ts-node": "^10.9.1"
  }
}
