{
  "name": "summary",
  "version": "1.0.0",
  "description": "Utility that uses madge to build a dependency tree.",
  "main": "./src/",
  "scripts": {
    "default-test": "echo \"Error: no test specified\" && exit 1",
    "test": "ts-node src/test.ts",
    "prepare": "echo \"preparing summary module \" && tsc -d -p ."
  },
  "author": "SAC",
  "license": "ISC",
  "dependencies": {
    "@types/madge": "^5.0.0",
    "@types/node": "^18.11.18",
    "madge": "^6.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
